# Application Name displayed in the UI
# Arayüzde gösterilen Uygulama Adı
VITE_APP_NAME="Simple POS"

# Backend API URL (Leave empty for relative path in Docker/Proxy setup)
# Backend API URL'i (Docker/Proxy kurulumunda göreceli yol için boş bırakın)
# For local dev without docker: http://localhost:3000
# Docker olmadan yerel geliştirme için: http://localhost:3000
VITE_API_URL=""

# Idle timeout in milliseconds before auto-lock (Default: 15 mins)
# Otomatik kilitleme öncesi boşta bekleme süresi milisaniye cinsinden (Varsayılan: 15 dk)
VITE_IDLE_TIMEOUT=900000

# Tax Rate (e.g. 0.20 for 20%)
# Vergi Oranı (örn: %20 için 0.20)
VITE_TAX_RATE=0.20
